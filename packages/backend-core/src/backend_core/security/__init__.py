"""Authentication, authorisation and safe I/O (§39-§40, §61).

Password hashing, token issue and verification, RBAC checks, and the
SSRF-safe fetcher every user-supplied URL must pass through — DNS and IP
validation blocking loopback, RFC1918 and cloud metadata addresses.

Populated from PHASE 3.
"""
