{
  "name": "@aipvs/ui",
  "version": "0.0.0",
  "private": true,
  "description": "Cross-app UI primitives and the Tailwind class-merge helper shared by every AIPVS frontend surface",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./cn": "./src/lib/cn.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "echo \"(lint runs via the web app's flat config; populated in P3-T09)\"",
    "test": "echo \"(no tests yet — populated alongside the first shared component)\""
  },
  "dependencies": {
    "clsx": "^2.1.1",
    "tailwind-merge": "^3.6.0"
  },
  "devDependencies": {
    "@aipvs/config": "workspace:*",
    "typescript": "^5.9.3"
  }
}
